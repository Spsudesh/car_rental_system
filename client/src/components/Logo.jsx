import React from "react";

const Logo = () => (
  <div className="text-2xl md:text-3xl font-bold bg-gradient-to-r from-primary via-purple-600 to-primary bg-clip-text text-transparent hover:scale-105 transition-transform">
    CarRental System
  </div>
);

export default Logo;
